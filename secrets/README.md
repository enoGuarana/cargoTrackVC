# Secrets locais

Esta pasta fica reservada para chaves e certificados usados em desenvolvimento local.

Nao versionar arquivos reais de chave privada. Para testes, use os arquivos em
`tests/fixtures/` ou informe `CERT_PATH` e `KEY_PATH` apontando para os seus
proprios arquivos locais.
